/** Clave de `localStorage` para la lista serializada de tareas. */
export const STORAGE_KEY = 'tasks';

/** Clave de `localStorage` para la preferencia de tema (`dark` | `light`). */
export const THEME_KEY = 'taskflow-theme';

/** Clave de `localStorage` para el modo de ordenación de la lista. */
export const SORT_MODE_KEY = 'taskflow-sort-mode';

/** Longitud máxima permitida para el título de una tarea (caracteres). */
export const MAX_TASK_TITLE_LENGTH = 200;

/** Longitud mínima tras normalizar espacios. */
export const MIN_TASK_TITLE_LENGTH = 1;
